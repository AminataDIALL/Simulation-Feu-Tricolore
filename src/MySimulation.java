

import simulator.RootCoordinator;
import exception.DEVS_Exception;
	
public class MySimulation {

	public static void main(String[] args) {
		// Creation of the study
		/*Feu_de_Circulation_System mystudy = new Feu_de_Circulation_System("TLS","Experimentation of a traffic light system");
		
		// Creation of the simulation tree
		RootCoordinator rooter = new RootCoordinator(mystudy.getSimulator());
		
		// Experimentation:
		// initial time is 0.0
		// final time is 1000.0
		try {
			rooter.init(0.0);
			rooter.run(1000.0);
		} catch (DEVS_Exception e) {
			e.printStackTrace();
			System.out.println(e.getMessage());
		}	*/
	}
}
